///
/// HybridMapViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridMapViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::karta::views {

  extern const char HybridMapViewComponentName[] = "MapView";

  HybridMapViewProps::HybridMapViewProps(const react::PropsParserContext& context,
                                         const HybridMapViewProps& sourceProps,
                                         const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    latitude([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("latitude", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.latitude;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.latitude);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MapView.latitude: ") + exc.what());
      }
    }()),
    longitude([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("longitude", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.longitude;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.longitude);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MapView.longitude: ") + exc.what());
      }
    }()),
    zoom([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("zoom", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.zoom;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.zoom);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MapView.zoom: ") + exc.what());
      }
    }()),
    children([&]() -> CachedProp<std::optional<std::variant<MapViewChildren, std::vector<MapViewChildren>>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("children", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.children;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::variant<MapViewChildren, std::vector<MapViewChildren>>>>::fromRawValue(*runtime, value, sourceProps.children);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MapView.children: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMapViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMapViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MapView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridMapViewProps::HybridMapViewProps(const HybridMapViewProps& other):
    react::ViewProps(),
    latitude(other.latitude),
    longitude(other.longitude),
    zoom(other.zoom),
    children(other.children),
    hybridRef(other.hybridRef) { }

  bool HybridMapViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("latitude"): return true;
      case hashString("longitude"): return true;
      case hashString("zoom"): return true;
      case hashString("children"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridMapViewComponentDescriptor::HybridMapViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridMapViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                   const std::shared_ptr<const react::Props>& props,
                                                                                   react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridMapViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridMapViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridMapViewShadowNode&>(shadowNode);
    const HybridMapViewProps& props = concreteShadowNode.getConcreteProps();
    HybridMapViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::karta::views
